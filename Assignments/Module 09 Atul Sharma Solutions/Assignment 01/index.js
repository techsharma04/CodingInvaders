let obj = {
    name: "Vishal",
    class: "X",
    seatNo: 987
}
console.log(Object.keys(obj).join(","));













// else if (m > 1 || m < 12) {
//     let monthKeys = Object.keys(monthObj);
//         let monthValues = Object.values(monthObj);
//         for (let i = 0; i < monthKeys.length; i++) {
//           if (monthKeys[i] === m && d > monthValues) {
//             
//           }
//         }